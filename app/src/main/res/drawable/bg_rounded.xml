<!-- res/drawable/rounded_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent"/> <!-- màu nền -->
    <corners android:radius="7dp"/> <!-- độ cong -->
    <stroke android:width="1dp" android:color="#DDDDDD"/> <!-- viền (có thể bỏ nếu không cần) -->
</shape>
